import { MainStyles } from "../../styles/index";

let styles = new MainStyles();

styles = Object.assign(styles, {
    animatedView: {
        position: "relative",
        marginTop: -176,
        zIndex: 3
    }
});

export { styles };
